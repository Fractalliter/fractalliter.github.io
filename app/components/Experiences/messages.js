/*
 * Experiences Messages
 *
 * This contains all the text for the Experiences component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Experiences';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Experiences',
  },
});

/*
 * Socials Messages
 *
 * This contains all the text for the Socials component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Socials';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Socials component!',
  },
});
